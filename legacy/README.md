# Prototipo anterior

Esqueleto original del equipo (P1–P4: `pipeline/`, `main.py`, `outputs/`, `tests/`, `ver.py`, `requirements.txt`, `entradas/`, `estado/`). Se conserva por trazabilidad y quedó sustituido por `factu-control/`; los cambios de raíz ya no construyen encima. Las carpetas y ficheros que aún están referenciados desde `factu-control/` (`pipeline/`, `main.py`, `outputs/`, `tests/`) permanecen en la raíz para no romper esos enlaces.
